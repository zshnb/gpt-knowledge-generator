'use client'
import TextFieldInput from "@/components/TextFieldInput";

export default function Home() {
  return (
    <main className="flex justify-center items-center h-full">
      <TextFieldInput value={1} onChange={() => {

      }}/>
    </main>
  )
}
